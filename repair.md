【背景说明】
我们正在重构企业设施报修系统的 AI 视觉识别模块。目前大模型返回了太多对用户无用的内部指标（如置信度、紧急程度）。我们需要精简产品体验：去掉前端对置信度、紧急程度、处理时间的展示。大模型只需要根据图片输出 1 到 3 个“可能的维修问题选项”，由用户在前端点击选择确认。如果模型非常确定，就只输出 1 个选项。

【任务分解与执行指南】

Step 1：重构大模型的 System Prompt 与 JSON Schema（后端）
请修改现有的 REPAIR_AGENT_SYSTEM_PROMPT。

保留其内部推理过程（CoT），因为这保证了准确率，但这部分无需传给前端展示。

移除原有的 confidence、urgency 字段。

修改输出结构：将原先单条结果改为一个名为 suggested_options 的数组（Array）。要求模型：如果完全确信，数组里只放 1 个对象；如果存在歧义（例如门看起来完好，可能是卡住也可能是锁坏了），数组里放入 2-3 个对象供用户选择。

新的 JSON Schema 结构如下，请更新对应的 Pydantic Model 或类型定义：

JSON
{
    "internal_reasoning": "str (你的观察和推理过程，不展示给用户)",
    "suggested_options": [
        {
            "category": "str (维修大类，用于后台派单)",
            "frontend_display_text": "str (一句自然流畅的报修文案，如：'窗帘拉珠卡住了，拉不下来')"
        }
    ]
}
Step 2：修改后端 API 接口解析逻辑（后端）
更新接收大模型响应的路由/控制器函数。确保接口返回给前端的数据结构是一个清晰的选项列表。过滤掉 internal_reasoning 等敏感/冗余信息，只把 suggested_options 传递给前端。

Step 3：重构前端 UI 展示逻辑（前端）
根据我上传的截图（原先有“分析成功”、“问题分类”、“紧急程度”、“置信度”卡片），请进行以下 UI 改造：

删除所有关于“紧急程度”、“置信度”、“处理时间”的卡片或标签。

改造结果区域：

如果 suggested_options 数组长度为 1：直接展示该文案，并配一个【确认报修】按钮。

如果 suggested_options 数组长度 > 1：使用单选框（Radio Button）或可点击的选项卡片（Selectable Cards）垂直排列这几个选项，并在顶部增加提示语：“我们发现了多种可能，请选择最符合您遇到的情况：”。用户点击选中一项后，【确认报修】按钮才高亮可用。

【行动要求】
请先向我确认你理解了上述逻辑，然后按照 Step 1 到 Step 3 逐步给出修改后的代码。